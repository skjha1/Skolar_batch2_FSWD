```Javascript
var myArray = [] // Empty array
var myNumbers = [1, 2, 3, 4, 5]; // Array with numbers
var myString = ['apple', 'banana', 'orange']; // Array with string


myArray.push(6); // adds 6 to the end of array
var lastElement = myArray.pop(); // Remove and return the last element of the array


var firstElement = myArray[0];
var firstElement = myArray[2];


var myObject = {}; // Empty object
var paerson = {
    name: "Harpreet",
    age: 22,
    city: "Delhi"
}; // object with properties


person.name = "Nikhil";
person.age = 20;



```
